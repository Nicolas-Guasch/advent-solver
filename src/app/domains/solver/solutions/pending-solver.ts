import { Solution } from './solution';

export class PendingSolver extends Solution {
  override partOne(input: string): string {
    throw new Error('Method not implemented.');
  }
  override partTwo(input: string): string {
    throw new Error('Method not implemented.');
  }
}
